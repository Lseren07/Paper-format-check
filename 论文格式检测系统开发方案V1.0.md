# 论文格式检测系统开发方案 V1.2

## 系统范围

### 1. 第一版本实现功能
系统 V1.0 实现：

#### （1）论文上传功能
支持教师上传：.docx 格式论文文件。

#### （2）Word 文档解析功能
系统自动解析：
- 文档结构
- 标题层级
- 字体
- 字号
- 段落格式
- 行距
- 缩进
- 页眉页脚
- 页码
- 图表信息

#### （3）论文格式检测功能
检测范围包括：
- 页面格式检测
- 标题格式检测
- 正文格式检测
- 摘要格式检测
- 目录一致性检测
- 标题编号连续性检测
- 图表格式检测
- 页码连续性检测
- 基础参考文献格式检测

#### （4）检测报告生成
系统生成：网页检测结果；PDF 检测报告。

PDF 报告包含：
- 错误列表
- 错误位置
- 错误文本定位
- 当前格式
- 规范要求
- 学校规范摘要

#### （5）被检测文档格式解析输出功能
系统能够输出：
- 文档格式结构数据
- 格式文字说明

用于：
- 查看论文当前格式
- 分析新旧论文规范差异
- 辅助更新检测规则

### 2. 第一版本暂不实现功能
暂不实现：
- 查重功能
- 自动修改论文格式
- 多学校模板
- 批量论文上传
- 学生账号体系
- 历史检测记录管理

## 三、用户角色设计

### 1. 教师用户
主要使用者。

功能：
- 启动系统
- 上传论文
- 查看检测结果
- 查看格式分析结果
- 下载 PDF 检测报告

### 2. 管理员模式（预留）
第一版本不开发管理员网页。采用配置文件方式管理。

管理员负责：
- 修改学校格式规则
- 更新 JSON 规则文件
- 维护检测标准

## 四、系统业务流程
教师启动系统 → 浏览器访问系统 → 上传论文.docx → 系统临时保存文件 → Word 解析模块解析论文 → 规则检测模块执行检测 → 生成检测结果 → 网页展示 → 生成 PDF 报告 → 删除临时论文文件

## 五、功能模块设计

### 1. 文件上传模块
功能：
- 接收教师上传论文
- 判断文件格式
- 创建检测任务
- 保存临时文件

输入：论文.docx 输出：检测任务。

### 2. Word 文档解析模块
功能：解析 Word 文件内部结构。

主要解析：
1. 文档结构：封面、摘要、目录、正文、参考文献
2. 格式信息：字体、字号、加粗、对齐方式、行距、缩进、页眉、页脚、页码
3. 标题信息：标题等级；标题编号；标题文本。

### 3. 格式检测模块
根据学校规范 JSON 文件进行检测。

检测内容：

#### （1）页面格式
检测：页边距；页眉；页脚；页码存在性；页码连续性。

#### （2）标题格式
检测：一级标题；二级标题；三级标题。
检测项：字体；字号；加粗；对齐方式；编号格式。

新增：标题编号连续性检测

例如： 错误：第一章 绪论 1.1 研究背景 1.3 研究意义
系统提示：标题编号存在缺失： 缺少 1.2 章节

#### （3）正文格式
检测：字体；字号；行距；段间距；首行缩进；对齐方式。

#### （4）目录一致性检测
检测：目录标题是否与正文一致；标题层级是否匹配；页码是否对应。

例如： 目录：第二章 系统设计 正文：第二章 系统实现
系统提示：目录标题与正文标题不一致

#### （5）图表格式检测
检测：图编号；表编号；图标题；表标题；图表字体格式。

#### （6）参考文献格式检测
第一版本检测：是否存在参考文献；编号格式；基础格式。

## 六、错误定位设计
第一版本采用：文本片段级错误定位。

检测到错误后，不仅显示章节，而显示错误附近文本。

示例：

定位文本：“随着人工智能技术的发展，计算机领域产生了大量新的研究方向……”
问题：正文段落字体格式错误 当前格式：黑体，小四号 规范要求：宋体，小四号
规范依据：《本科毕业论文格式规范》第 X 条

实现方式： 系统解析 Word 段落结构，为每个段落建立索引。发现错误时，提取错误段落前若干字符作为定位信息。

## 七、被检测文档格式解析输出功能

### 1. 功能目标
系统解析论文当前使用的格式结构，并输出：结构化格式数据；人类可读格式说明。

### 2. 功能流程
上传论文.docx → Word 格式解析 → 提取格式信息 → 生成 JSON 格式数据 → 生成文字说明报告

### 3. 格式数据输出
```json
{
  "body": {
    "font": "宋体",
    "size": "小四",
    "line_spacing": "1.5",
    "indent": "2字符"
  },
  "title1": {
    "font": "黑体",
    "size": "三号",
    "align": "center"
  }
}
```

### 4. 格式文字说明输出
正文格式 当前格式： 字体：宋体 字号：小四 行距：1.5 倍 首行缩进：2 字符

一级标题格式 当前格式： 字体：黑体 字号：三号 对齐方式：居中

### 5. 应用场景
主要用于：
- 查看论文实际格式
- 对比新旧论文规范
- 辅助更新检测规则
- 分析学校模板变化

该功能不负责自动修改论文格式。

## 八、规则库设计
系统采用 JSON 文件保存学校论文格式规范。

示例：
```json
{
  "body":{
    "font":"宋体",
    "size":"小四",
    "line_spacing":"1.5"
  },
  "title1":{
    "font":"黑体",
    "size":"三号"
  }
}
```

检测模块读取规则，并与论文实际格式进行比较。

## 九、系统架构设计
浏览器 → React 前端 → FastAPI 后端 → Word 解析模块、格式检测模块、规则管理模块、格式解析输出模块、PDF 生成模块

## 十、文件管理策略
论文文件
- 上传：临时保存。
- 检测完成：删除论文文件。
- 系统：不长期保存学生论文。

检测报告
- 生成：PDF 文件。
- 由教师自行下载保存。

## 十一、后续迭代规划

### V2.0
目标：提升检测深度和使用效率。

计划增加：
- 更细节的问题检测；包括：空格问题；多余换行；格式异常；更复杂段落问题。
- 更完善标题结构分析；
- 更复杂参考文献检测；
- 批量论文检测；
- 历史检测记录管理；
- 学生自主检测模式。
```

直接复制上面整个块，粘贴到你的 `.md` 文件即可。