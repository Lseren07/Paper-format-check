# 论文格式检测系统 API接口详细设计文档 V1.0
> 版本：V1.0
> 项目名称：论文格式检测系统
> 文档类型：接口设计文档

## 修订记录
| 版本 | 日期 | 修改内容 |
|------|------|----------|
| V1.0 | 2026-09-07 | 完成API接口详细设计 |

## 目录
1. [接口设计规范](#接口设计规范)
2. [通用数据结构](#通用数据结构)
3. [文件上传接口](#文件上传接口)
4. [创建检测任务接口](#创建检测任务接口)
5. [查询检测状态接口](#查询检测状态接口)
6. [获取检测结果接口](#获取检测结果接口)
7. [获取格式分析结果接口](#获取格式分析结果接口)
8. [生成检测报告接口](#生成检测报告接口)
9. [下载检测报告接口](#下载检测报告接口)
10. [删除检测任务接口](#删除检测任务接口)
11. [错误码设计](#错误码设计)
12. [接口调用流程](#接口调用流程)
13. [后续扩展接口](#后续扩展接口)

## 接口设计规范
### 1.1 接口架构
系统采用 RESTful API 设计。

通信方式：
HTTP + JSON

接口基础路径：
`/api/v1`

完整示例：
`http://server/api/v1/paper/upload`

### 1.2 请求规范
- JSON接口：
`Content-Type: application/json`
- 文件上传接口：
`Content-Type: multipart/form-data`

### 1.3 返回规范
所有接口统一返回格式：
```json
{
    "code":200,
    "message":"success",
    "data":{}
}
```

字段说明：

| 字段 | 类型 | 说明 |
|------|------|------|
| code | int | 状态码 |
| message | string | 返回信息 |
| data | object | 业务数据 |

### 1.4 状态码设计

| 状态码 | 说明 |
|--------|------|
| 200 | 请求成功 |
| 400 | 参数错误 |
| 401 | 未授权 |
| 404 | 资源不存在 |
| 422 | 数据验证失败 |
| 500 | 服务器内部错误 |

## 通用数据结构
### 2.1 Task任务对象
检测任务对象。
```json
{
    "task_id":"TASK20260907001",
    "filename":"毕业论文.docx",
    "status":"running",
    "create_time":"2026-09-07 10:00:00"
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| task_id | string | 任务编号 |
| filename | string | 论文文件名 |
| status | string | 任务状态 |
| create_time | string | 创建时间 |

### 2.2 任务状态

| 状态 | 说明 |
|------|------|
| uploaded | 文件已上传 |
| parsing | 解析中 |
| detecting | 检测中 |
| completed | 完成 |
| failed | 失败 |

### 2.3 Error错误对象
```json
{
    "error_id":"ERR001",
    "type":"font_error",
    "location":"第二章 第一节",
    "content":"这是错误文本",
    "current":"黑体 小四",
    "expected":"宋体 小四"
}
```

| 字段 | 说明 |
|------|------|
| error_id | 错误编号 |
| type | 错误类型 |
| location | 错误位置 |
| content | 错误文本 |
| current | 当前格式 |
| expected | 要求格式 |

## 文件上传接口
### 3.1 上传论文文件
- 接口：`POST /api/v1/paper/upload`
- 功能：上传待检测论文。
- 请求类型：`multipart/form-data`

请求参数：

| 参数 | 类型 | 必须 | 说明 |
|------|------|------|------|
| file | File | 是 | docx论文文件 |

请求示例：
> file: 毕业论文.docx

成功返回 HTTP 200 OK
```json
{
    "code":200,
    "message":"上传成功",
    "data":{
        "task_id":"TASK001"
    }
}
```

失败返回
```json
{
    "code":400,
    "message":"文件格式错误",
    "data":null
}
```

## 创建检测任务接口
### 4.1 开始检测
- 接口：`POST /api/v1/detect/start`
- 功能：根据上传文件创建检测任务。

请求参数
```json
{
    "task_id":"TASK001"
}
```

返回
```json
{
    "code":200,
    "message":"检测任务创建成功",
    "data":{
        "task_id":"TASK001",
        "status":"parsing"
    }
}
```

## 查询检测状态接口
### 5.1 查询任务状态
- 接口：`GET /api/v1/detect/status/{task_id}`
- 示例：`GET /api/v1/detect/status/TASK001`

返回
```json
{
    "code":200,
    "data":{
        "task_id":"TASK001",
        "status":"detecting",
        "progress":60
    }
}
```

| 字段 | 说明 |
|------|------|
| progress | 检测进度百分比 |

## 获取检测结果接口
### 6.1 获取检测结果
- 接口：`GET /api/v1/detect/result/{task_id}`

返回
```json
{
    "code":200,
    "data":{
        "task_id":"TASK001",
        "total_error":3,
        "errors":[
            {
                "type":"font_error",
                "location":"摘要",
                "expected":"宋体"
            }
        ]
    }
}
```

## 获取格式分析结果接口
### 7.1 获取论文格式结构
- 接口：`GET /api/v1/document/analysis/{task_id}`
- 功能：返回被检测论文当前格式。

返回示例
```json
{
    "code":200,
    "data":{
        "page":{
            "margin":"2.5cm"
        },
        "body":{
            "font":"宋体",
            "size":"小四",
            "line_spacing":"1.5"
        },
        "title":{
            "font":"黑体",
            "size":"三号"
        }
    }
}
```

## 生成检测报告接口
### 8.1 创建PDF报告
- 接口：`POST /api/v1/report/create`

请求
```json
{
    "task_id":"TASK001"
}
```

返回：
```json
{
    "code":200,
    "data":{
        "report_id":"REPORT001"
    }
}
```

## 下载检测报告接口
### 9.1 下载PDF
- 接口：`GET /api/v1/report/download/{report_id}`
- 返回：`application/pdf`

## 删除检测任务接口
### 10.1 删除任务
- 接口：`DELETE /api/v1/task/{task_id}`
- 功能：删除临时文件和检测数据。

返回：
```json
{
    "code":200,
    "message":"删除成功"
}
```

## 错误码设计
> 见 1.4 状态码设计

## 接口调用流程
完整流程：
```
用户
  ↓ 上传论文  POST /api/v1/paper/upload
  ↓ 创建任务  POST /api/v1/detect/start
  ↓ 查询状态  GET /api/v1/detect/status/{task_id}
  ↓ 获取结果  GET /api/v1/detect/result/{task_id}
  ↓ 查看格式分析  GET /api/v1/document/analysis/{task_id}
  ↓ 生成报告  POST /api/v1/report/create
  ↓ 下载报告  GET /api/v1/report/download/{report_id}
```

## 后续扩展接口
未来版本预留：
- 用户接口
  - `/user/login`
- 学校规则接口
  - `/rule/list`
- 历史记录接口
  - `/history/list`
- 批量检测接口
  - `/batch/upload`

## 总结
本接口设计文档定义论文格式检测系统 V1.0 所需全部核心 API。
接口设计遵循 RESTful 规范，实现：
- 文件上传；
- 检测任务管理；
- 检测状态查询；
- 检测结果获取；
- 格式分析输出；
- PDF报告生成。

---

