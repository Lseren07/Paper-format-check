# 论文格式检测系统开发分工方案

## 一、项目背景

根据《论文格式检测系统开发方案 V1.0》，系统主要包含：

-   论文上传功能
-   Word 文档解析功能
-   论文格式检测功能
-   检测报告生成功能
-   被检测文档格式解析输出功能
-   规则库管理功能

系统采用前后端不分离的协同开发方式，两名开发人员均负责完整业务模块，包括对应的前端页面、后端接口以及核心业务逻辑。

------------------------------------------------------------------------

# 二、总体分工原则

采用：

> 按业务模块划分，而不是单纯按照前端和后端划分。

原因：

-   Word 文档解析和格式检测属于系统核心技术，开发难度较高；
-   前端页面数量较少，单独负责前端会导致工作量不均衡；
-   每个人负责完整模块，有利于后期维护和项目答辩说明。

------------------------------------------------------------------------

# 三、人员分工

## 李维嘉（主要负责：论文解析与格式检测核心模块）

## 1. 后端开发

### （1）Word 文档解析模块

负责：

-   .docx 文件读取；
-   Word 内部结构解析；
-   提取论文结构信息。

解析内容：

-   封面；
-   摘要；
-   目录；
-   正文；
-   参考文献；
-   标题层级；
-   标题编号；
-   字体；
-   字号；
-   段落格式；
-   行距；
-   缩进；
-   页眉页脚；
-   页码；
-   图表信息。

技术方向：

-   python-docx；
-   XML 解析。

------------------------------------------------------------------------

### （2）格式检测模块

负责实现论文格式规则检测。

检测内容：

-   页面格式检测；
-   标题格式检测；
-   正文格式检测；
-   摘要格式检测；
-   目录一致性检测；
-   标题编号连续性检测；
-   图表格式检测；
-   参考文献基础格式检测。

主要工作：

-   读取 JSON 格式规范规则；
-   对比论文实际格式；
-   输出错误结果。

------------------------------------------------------------------------

### （3）被检测文档格式解析输出模块

负责：

-   提取论文当前格式数据；
-   生成结构化 JSON 数据；
-   输出人类可读格式说明。

输出示例：

``` json
{
  "body":{
    "font":"宋体",
    "size":"小四",
    "line_spacing":"1.5"
  }
}
```

文字说明：

    正文格式：
    字体：宋体
    字号：小四
    行距：1.5倍

------------------------------------------------------------------------

## 2. 前端开发

负责：

### 格式分析页面

功能：

-   展示论文格式结构；
-   展示格式解析结果；
-   查看当前论文格式。

### 检测结果页面

功能：

-   展示格式错误；
-   查看错误详情。

------------------------------------------------------------------------

# 张耀匀（主要负责：系统交互与报告模块）

## 1. 后端开发

### （1）文件上传模块

负责：

-   接收教师上传论文；
-   判断文件格式；
-   创建检测任务；
-   临时保存文件。

流程：

    上传论文.docx
            |
    文件校验
            |
    创建检测任务
            |
    进入检测流程

------------------------------------------------------------------------

### （2）错误定位模块

负责：

实现错误位置定位。

展示：

    错误位置：
    第二章 系统设计

    错误文本：
    随着人工智能技术的发展……

    问题：
    正文字体格式错误

    当前格式：
    黑体，小四

    规范要求：
    宋体，小四

实现：

-   保存段落索引；
-   提取错误附近文本；
-   输出定位信息。

------------------------------------------------------------------------

### （3）PDF 检测报告生成模块

负责：

生成检测报告。

报告包含：

-   错误列表；
-   错误位置；
-   错误文本；
-   当前格式；
-   规范要求；
-   学校规范摘要。

------------------------------------------------------------------------

### （4）规则库管理模块

负责：

-   JSON 规则文件管理；
-   读取学校论文格式规范；
-   提供检测规则支持。

------------------------------------------------------------------------

## 2. 前端开发

负责：

### 首页

功能：

-   系统入口；
-   上传论文入口。

------------------------------------------------------------------------

### 上传页面

功能：

-   文件选择；
-   上传进度显示；
-   检测任务创建。

------------------------------------------------------------------------

### 检测结果展示页面

功能：

-   查看错误数量；
-   查看错误列表；
-   下载 PDF 报告。

------------------------------------------------------------------------

# 四、公共开发内容

## 1. 数据结构设计

共同确定：

-   上传任务数据结构；
-   检测结果 JSON 格式；
-   错误信息格式；
-   文件管理方式。

------------------------------------------------------------------------

## 2. 项目架构搭建

共同维护：

    paper-format-check/

    ├── backend
    │
    ├── frontend
    │
    ├── rules
    │
    ├── uploads
    │
    └── reports

------------------------------------------------------------------------

## 3. 系统测试

共同准备：

测试论文：

-   正常格式论文；
-   存在格式错误论文。

测试内容：

-   上传测试；
-   Word解析测试；
-   格式检测测试；
-   PDF生成测试。

------------------------------------------------------------------------


# 六、推荐开发顺序

> 当前实现范围先限定为 `.docx` 文件；旧版 `.doc` 不纳入本阶段开发。

1.  两人共同完成项目框架搭建；
2.  开发者A完成 Word 解析基础能力；
3.  开发者B完成上传和任务流程；
4.  两人联调检测接口；
5.  完成结果展示；
6.  完成 PDF 报告生成；
7.  进行系统测试。
